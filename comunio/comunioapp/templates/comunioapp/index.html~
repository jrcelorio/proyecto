<html>
	<head>
		<title>Comunio</title>
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
			<a href="{% url 'addalineacion' %}">Añadir Alineación</a>
		{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		{% if lista_equipos %}
			<table border="2">
				<tr>
					<td>Nombre</td>daline
					<td>Fecha</td>
				</tr>
				{% for equipo in lista_equipos %}
					<tr>
						<td><a href="{% url 'detalle_alineacion' alineacion.id %}">{{ equipo.nombre }}</a></td>
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay equipos</p>			
		{% endif %}
	</body>
</html>
