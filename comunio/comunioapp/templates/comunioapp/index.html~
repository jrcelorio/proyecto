<html>
	<head>
		<title>Comunio</title>
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
			<a href="{% url 'addalineacion' %}">Añadir Alineación</a>
		{% endif %}
		<nav>
			
		</nav>
		{% if lista_equipos %}
			<table border="2">
				<tr>
					<td>Nombre</td>
					<td>Descripcion</td>
				</tr>
				{% for equipo in lista_equipos %}
					<tr>
						<td><a href="{% url 'detalle_alineacion' equipo.id %}">{{ equipo.nombre }}</a></td>
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay equipos</p>			
		{% endif %}
	</body>
</html>
